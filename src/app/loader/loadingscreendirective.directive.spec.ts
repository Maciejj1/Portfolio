import { LoadingscreendirectiveDirective } from './loadingscreendirective.directive';

describe('LoadingscreendirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingscreendirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
